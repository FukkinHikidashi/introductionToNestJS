name: echo_variables

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - id: test
        entrypoint: 'bash'
        args:
          - -c
          - |
            echo ${BRANCH_NAME}
