name: echo_variables

on: [push]

jobs:
  echo_variables:
    runs-on: ubuntu-latest

    steps:
      - id: test
        entrypoint: 'bash'
        args:
          - -c
          - |
            echo ${BRANCH_NAME}
